// Import all components to trigger customElements.define()
import "./components/todo-app.js";

// The app is rendered by placing <todo-app> in the HTML
// All component registration happens via imports above
